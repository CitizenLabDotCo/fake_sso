<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <script type='text/javascript' src='/javascripts/index.js'></script>
</head>

<body>
  <div class="main-container">
    <h1>
      <%= title %>
    </h1>
    <main>
      <div class="field-container">
        <label for="profile-select">Profile:</label>
        <select name="profile-select" id="profile-select">
          <% profiles.forEach(function(profile) { %>
            <option value="<%= profile.id %>">
              <%= profile.description %>
            </option>
            <% }); %>
        </select>
      </div>
      <div class="field-container">
        <label for="email-input">Email (optional override):</label>
        <input type="email" id="email-input" placeholder="Leave blank to use profile default" />
      </div>
      <div class="field-container">
        <label for="sub-input">Sub (optional override):</label>
        <input type="text" id="sub-input" placeholder="Leave blank to use profile default" />
      </div>
      <button id="submit-button">Submit SSO</button>
    </main>
  </div>
</body>

</html>